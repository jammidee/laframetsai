#==========================================================
# Security
# If it is required for this app to authenticate the user.
#==========================================================
#JWT 09/28/2023
BASIC_USERNAME=jamesweb
BASIC_PASSWORD=T3l3sc0p3@!!
JWT_SECRET=F4m3!
TOKEN_EXPIRE=1h

#=============================================================
# Ollama Model Configuration
# Used this to customized Sophia
#=============================================================
AI_MASTER_MODEL=llama2
AI_MASTER_HOST=127.0.0.1
AI_MASTER_PORT=11434

AI_IMAGE_MODEL=llava
AI_IMAGE_HOST=127.0.0.1
AI_IMAGE_PORT=11434

AI_EMBED_MODEL=nomic-embed-text
AI_EMBED_HOST=127.0.0.1
AI_EMBED_PORT=11434

AI_TOOLING_MODEL=mistral
AI_TOOLING_HOST=127.0.0.1
AI_TOOLING_PORT=11434

AI_EXTRA_MODEL=ANY
AI_EXTRA_HOST=127.0.0.1
AI_EXTRA_PORT=11434

# AI Query Settings
AI_TEMPERATURE=0.7
AI_MAX_TOKENS=-1

# VECTOR DATABASE (ChromaDB)
VEC_EMBED_HOST=127.0.0.1
VEC_EMBED_PORT=8000
VEC_CHUNK=1000
VEC_CHUNK_OVERLAP=200

COLLECTION_NAME=sophia-collection

#=============================================================
# Database Configuration
# Used only of the app will be accesing the database directly
# and not via API calls to an application server.
#=============================================================
# Database Settings
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=db_dev
DB_USERNAME=root
DB_PASSWORD=password

#============================================================
# Application Server.
# This is the source of the app all data and business logic.
#============================================================
APP_PROTOCOL=http
APP_HOST=127.0.0.1
APP_PORT=4140

# Server Settings
PORT=3000
APP_SERVICE_INTERVAL=10000

# Logging Service Settings
LOG_DIR='../../logs'

#Check for updates
APP_UPDATE_URL=https://example.com/version.txt
